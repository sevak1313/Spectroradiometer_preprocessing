After extracting data from spectroradiometer and integration sphere .txt files using the GUI code, we started with preprocessing. The first step was to use Savitzky-Golay filtering.

1. GUI extracted reflectance data from .txt files to Excel file. Each measurement of each trigger press is separate at this point (no averaging of multiple files per sample done yet.)
2. 
